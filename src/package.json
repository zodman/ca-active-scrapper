{
  "source": "index.html",
  "scripts": {
    "start": "rm -rf dist .parcel-cache && parcel",
    "build": "BUILD_TIME=$(date) rm -rf dist .parcel-cache &&  parcel build"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "parcel": "^2.13.3",
    "react": "^19.0.0",
    "react-calendar-timeline": "^0.30.0-beta.3",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "process": "^0.11.10"
  }
}
